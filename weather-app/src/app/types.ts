export interface WeatherData {
    name: string;
    description: string;
    current_temp: string;
    min_temp: string;
    max_temp: string;
    pressure: string;
    humidity: string;
    weather: string;
    feels_like: string;
}